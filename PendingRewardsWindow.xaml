<Window x:Class="MHRebornLauncher.PendingRewardsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Community Rewards Ready"
        Width="570" Height="700" MinWidth="500" MinHeight="480"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize" WindowStyle="None"
        Background="#111827" ShowInTaskbar="False">
    <Window.Resources>
        <Style x:Key="LauncherContentScrollBarStyle" TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="7"/>
            <Setter Property="MinWidth" Value="1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Margin" Value="3,3,3,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb MinHeight="38">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border Background="#626B7B" CornerRadius="4"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="8" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border Background="#111827" BorderBrush="#36516D" BorderThickness="1" CornerRadius="8" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="72"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#172337" BorderBrush="#36516D" BorderThickness="0,0,0,1" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="18,0,0,0">
                    <TextBlock Text="COMMUNITY REWARDS" Foreground="#C9D4E3" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button Content="✕" Width="46" Height="47" Padding="0" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Click="CloseButton_Click"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ScrollViewer.Resources>
                    <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource LauncherContentScrollBarStyle}"/>
                </ScrollViewer.Resources>
                <StackPanel Margin="30,26,30,24">
                    <Image Source="pack://application:,,,/MHRebornLauncher;component/Assets/OmegaNodeLogo.png" Width="92" Height="92" Stretch="Uniform" HorizontalAlignment="Center"/>
                    <TextBlock Text="REWARDS READY" Foreground="#75E6AD" FontSize="11" FontWeight="Bold" TextAlignment="Center" Margin="0,10,0,0"/>
                    <TextBlock Text="You earned Community Goal rewards" FontSize="25" FontWeight="Bold" TextAlignment="Center" TextWrapping="Wrap" Margin="0,6,0,0"/>
                    <TextBlock Text="Log into Marvel Heroes to receive the rewards listed below." Foreground="#B7C1D2" FontSize="13" TextAlignment="Center" TextWrapping="Wrap" Margin="18,8,18,22"/>
                    <StackPanel x:Name="ClaimsPanel"/>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2" Background="#172337" BorderBrush="#36516D" BorderThickness="0,1,0,0">
                <Grid Margin="24,0">
                    <TextBlock Text="Rewards are delivered after you enter the game." Foreground="#91A0B6" FontSize="11" VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,0,150,0"/>
                    <Button Content="GOT IT" Width="120" Height="40" HorizontalAlignment="Right" VerticalAlignment="Center" Click="CloseButton_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
