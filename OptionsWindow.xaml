<Window x:Class="MHRebornLauncher.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Launcher Options" Width="820" Height="700" MinWidth="760" MinHeight="620"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize" WindowStyle="None"
        Background="#111827" ShowInTaskbar="False" StateChanged="Window_StateChanged">
    <Window.Resources>
        <Style x:Key="OptionCardStyle" TargetType="Border"><Setter Property="Background" Value="#1B2638"/><Setter Property="BorderBrush" Value="#41506A"/><Setter Property="BorderThickness" Value="1"/><Setter Property="CornerRadius" Value="6"/><Setter Property="Padding" Value="18"/><Setter Property="Margin" Value="0,0,0,12"/></Style>
        <Style x:Key="LauncherContentScrollBarStyle" TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="7"/>
            <Setter Property="MinWidth" Value="1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Margin" Value="3,3,3,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb MinHeight="38">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border Background="#626B7B" CornerRadius="4"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="7" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Border Background="#111827" BorderBrush="#3A4658" BorderThickness="1" CornerRadius="7">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="58"/><RowDefinition Height="*"/><RowDefinition Height="64"/></Grid.RowDefinitions>
            <Border Grid.Row="0" Background="#182235" BorderBrush="#3A4658" BorderThickness="0,0,0,1" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="22,0,0,0">
                    <TextBlock Text="OPTIONS" FontSize="17" FontWeight="Bold" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                        <Button Content="—" Width="46" Height="32" Padding="0" Background="Transparent" Click="MinimizeWindowButton_Click"/>
                        <Button x:Name="MaximizeWindowButton" Content="□" Width="46" Height="32" Padding="0" Background="Transparent" Click="MaximizeWindowButton_Click"/>
                        <Button Content="✕" Width="46" Height="32" Padding="0" Background="Transparent" Click="CloseWindowButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions><ColumnDefinition Width="185"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="#152033" BorderBrush="#3A4658" BorderThickness="0,0,1,0" Padding="14,18">
                    <StackPanel>
                        <Button x:Name="GeneralNavButton" Content="GENERAL" HorizontalContentAlignment="Left" Background="#243142" BorderBrush="#148BEB" BorderThickness="3,0,0,0" Padding="16,12" Click="GeneralNavButton_Click"/>
                        <Button x:Name="SupportNavButton" Content="SUPPORT" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent" BorderThickness="3,0,0,0" Padding="16,12" Margin="0,6,0,0" Click="SupportNavButton_Click"/>
                    </StackPanel>
                </Border>
                <Grid Grid.Column="1" Background="#111827">
                    <ScrollViewer x:Name="GeneralPanel" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ScrollViewer.Resources>
                            <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource LauncherContentScrollBarStyle}"/>
                        </ScrollViewer.Resources>
                        <StackPanel Margin="28,24,30,26">
                            <TextBlock Text="General" FontSize="25" FontWeight="Bold"/>
                            <TextBlock Text="Choose how Marvel Heroes starts when you click Play." Foreground="{StaticResource MutedTextBrush}" Margin="0,5,0,22"/>
                            <Border Style="{StaticResource OptionCardStyle}"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel Margin="0,0,20,0"><TextBlock Text="Hide the startup splash" FontSize="15" FontWeight="SemiBold"/><TextBlock Text="Hides the small startup window shown before the game opens." Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,5,0,0"/></StackPanel><CheckBox x:Name="SkipLaunchSplashCheckBox" Grid.Column="1" VerticalAlignment="Center" Width="20" Height="20"/></Grid></Border>
                            <Border Style="{StaticResource OptionCardStyle}"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel Margin="0,0,20,0"><TextBlock Text="Skip intro videos" FontSize="15" FontWeight="SemiBold"/><TextBlock Text="Skips the opening videos so clicking Play takes you directly into the game." Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,5,0,0"/></StackPanel><CheckBox x:Name="SkipStartupMoviesCheckBox" Grid.Column="1" VerticalAlignment="Center" Width="20" Height="20"/></Grid></Border>
                            <Border Style="{StaticResource OptionCardStyle}"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel Margin="0,0,20,0"><TextBlock Text="Minimize launcher after Play" FontSize="15" FontWeight="SemiBold"/><TextBlock Text="Keeps the launcher running in the background while you play." Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,5,0,0"/></StackPanel><CheckBox x:Name="MinimizeAfterLaunchCheckBox" Grid.Column="1" VerticalAlignment="Center" Width="20" Height="20"/></Grid></Border>
                            <Border Style="{StaticResource OptionCardStyle}"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel Margin="0,0,20,0"><TextBlock Text="Restore launcher when the game closes" FontSize="15" FontWeight="SemiBold"/><TextBlock Text="Returns the launcher to the screen when your game session ends." Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,5,0,0"/></StackPanel><CheckBox x:Name="RestoreAfterGameExitCheckBox" Grid.Column="1" VerticalAlignment="Center" Width="20" Height="20"/></Grid></Border>
                            <Border Style="{StaticResource OptionCardStyle}" Margin="0"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel Margin="0,0,20,0"><TextBlock Text="Discord Rich Presence" FontSize="15" FontWeight="SemiBold"/><TextBlock Text="Shows [US] Marvel Heroes Reborn as your Discord activity while the launcher or game is open." Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,5,0,0"/></StackPanel><CheckBox x:Name="DiscordRichPresenceCheckBox" Grid.Column="1" VerticalAlignment="Center" Width="20" Height="20"/></Grid></Border>
                        </StackPanel>
                    </ScrollViewer>
                    <ScrollViewer x:Name="SupportPanel" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ScrollViewer.Resources>
                            <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource LauncherContentScrollBarStyle}"/>
                        </ScrollViewer.Resources>
                        <StackPanel Margin="28,24,30,26">
                            <TextBlock Text="Support" FontSize="25" FontWeight="Bold"/>
                            <TextBlock Text="Verify your installation, collect logs, and create a support package." Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,5,0,18"/>
                            <WrapPanel Margin="0,0,0,14">
                                <Button Content="VERIFY INSTALLATION" Margin="0,0,8,8" Click="VerifyInstallationButton_Click"/>
                                <Button Content="CREATE DIAGNOSTIC ZIP" Margin="0,0,8,8" Click="CreateDiagnosticZipButton_Click"/>
                                <Button Content="COPY SUPPORT SUMMARY" Margin="0,0,8,8" Background="#2A3445" Click="CopySupportSummaryButton_Click"/>
                            </WrapPanel>
                            <WrapPanel Margin="0,0,0,14">
                                <Button Content="OPEN GAME FOLDER" Margin="0,0,8,8" Background="#2A3445" Click="OpenGameFolderButton_Click"/>
                                <Button Content="OPEN GAME LOGS" Margin="0,0,8,8" Background="#2A3445" Click="OpenGameLogsButton_Click"/>
                                <Button Content="OPEN LAUNCHER LOGS" Margin="0,0,8,8" Background="#2A3445" Click="OpenLauncherLogsButton_Click"/>
                                <Button Content="OPEN DATA FOLDER" Margin="0,0,8,8" Background="#2A3445" Click="OpenDataFolderButton_Click"/>
                            </WrapPanel>
                            <Border Background="#0C1320" BorderBrush="#3A4658" BorderThickness="1" CornerRadius="6" Padding="14">
                                <StackPanel>
                                    <TextBlock x:Name="SupportStatusText" Text="Run Verify Installation to check this computer." Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                    <ItemsControl x:Name="DiagnosticResultsList">
                                        <ItemsControl.ItemTemplate><DataTemplate><Border Background="#182235" CornerRadius="4" Margin="0,0,0,7" Padding="10"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="160"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="{Binding Severity}" FontWeight="Bold" Foreground="{Binding SeverityBrush}"/><TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="SemiBold"/><TextBlock Grid.Column="2" Text="{Binding Details}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"/></Grid></Border></DataTemplate></ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
            <Border Grid.Row="2" Background="#182235" BorderBrush="#3A4658" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,22,0"><Button Content="CANCEL" Width="105" Margin="0,0,10,0" Background="#2A303C" Click="CancelButton_Click"/><Button Content="SAVE" Width="105" Click="SaveButton_Click"/></StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
